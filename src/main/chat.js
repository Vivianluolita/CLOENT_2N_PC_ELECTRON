/* eslint-disable no-unused-vars */
let { ipcMain: ipc, dialog, app } = require('electron')
let createChatWindow = require('./chatWindow')

module.exports = mainWindow => {
  // app.on('ready', () => {
  //   createWindow();
  // });
  // ipc.on('checkForUpdate', () => {
  //   console.log('check--update')
  // })
}
